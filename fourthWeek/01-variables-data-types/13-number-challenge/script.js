let x = Math.floot(Math.random() * 101); //number between 0 and 100
let y = Math.floot(Math.random() * 51); //number between 0 and 100

let sumOutput = x + y;
let differenceOutput = x - y;
let productOutput = x * y;
let quotientOutput = x / y;
let remainderOutput = x % y;
